<p id="notice"><%= notice %></p>

<h1>Elections</h1>

<div class="table-responsive">
  <table class="table">
    <thead>
      <tr>
        <th>Year</th>
        <th>Type</th>
        <th>Description</th>
        <th style="width: 300px"></th>
      </tr>
    </thead>

    <tbody>
      <% @elections.each do |election| %>
        <tr>
          <td><%= election.year %></td>
          <td><%= election.type_election %></td>
          <td><%= election.description %></td>
          <td>
            <%= link_to '<i class="glyphicon glyphicon-pencil"></i> Show'.html_safe, election, class: 'btn btn-info' %>
            <%= link_to '<i class="glyphicon glyphicon-pencil"></i> Edit'.html_safe, edit_election_path(election), class: 'btn btn-primary' %>
            <%= link_to '<i class="glyphicon glyphicon-remove"></i> Destroy'.html_safe, election, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger' %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>

<br>

<%= link_to '<i class="glyphicon glyphicon-file"></i> New Election'.html_safe, new_election_path, class: 'btn btn-primary' %>
