<p id="notice"><%= notice %></p>

<p>
  <strong>Year:</strong>
  <%= @election.year %>
</p>

<p>
  <strong>Type:</strong>
  <%= @election.type %>
</p>

<p>
  <strong>Description:</strong>
  <%= @election.description %>
</p>

<%= link_to '<i class="glyphicon glyphicon-pencil"></i> Edit'.html_safe, edit_election_path(@election), class: 'btn btn-primary' %>

<%= link_to '<i class="glyphicon glyphicon-chevron-left"></i> Back'.html_safe, elections_path, class: 'btn btn-primary' %>
