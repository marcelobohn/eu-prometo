<h1><%= I18n.t('views.election.index.show') %></h1>

<p>
  <strong><%= I18n.t('views.election.columns.year') %>:</strong>
  <%= @election.year %>
</p>

<p>
  <strong><%= I18n.t('views.election.columns.type') %>:</strong>
  <%= @election.type_election_text %>
</p>

<p>
  <strong><%= I18n.t('views.election.columns.description') %>:</strong>
  <%= @election.description %>
</p>

<%= link_to '<i class="glyphicon glyphicon-pencil"></i> '.html_safe+I18n.t('views.edit'), edit_election_path(@election), class: 'btn btn-primary' %>

<%= link_to '<i class="glyphicon glyphicon-chevron-left"></i> '.html_safe+I18n.t('views.back'), elections_path, class: 'btn btn-primary' %>
