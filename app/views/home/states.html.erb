<div class="container">
  <span class="title-resource">Estados</span>

  <hr />

  <div class="pull-left">
    <%= form_tag states_path, :method => 'get' do %>
      <div class="input-group col-md-3">
        <%= text_field_tag :search, params[:search], class: 'form-control', style: 'width:90%' %>
        <span class="input-group-btn">
          <%= button_tag type: 'submit', class: 'btn btn-primary btn-sm' do %>
            <i class="glyphicon glyphicon-search"></i> Pesquisa
          <% end %>
        </span>
      <% end %>
    </div>
  </div>

  <br />
  <br />
  <br />

  <% @managers.each do |manager| %>
    <%= render 'manager', manager: manager %>
  <% end %>

  <% if (@managers.count == 0) %>
    <div>
      Nenhum Governador foi encontrado na pesquisa
    </div><br /><br />
  <% else %>
    <%= paginate @managers %>
  <% end %>
</div>
